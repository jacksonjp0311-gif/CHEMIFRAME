# RCC CONTEXT SURFACE

This repository uses bounded module docs rather than monolithic source-dump context.
Each module is expected to evolve with:
- specification
- hooks
- artifacts
- theory
- invariants
- examples